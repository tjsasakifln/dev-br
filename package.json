{
  "name": "open-swe",
  "author": "LangChain",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "setup": "npm install && npm run db:migrate",
    "dev:platform": "turbo dev --filter=@open-swe/web",
    "build:platform": "turbo build --filter=@open-swe/web",
    "test:platform": "echo 'Nenhum teste de plataforma configurado' && exit 0",
    "lint:platform": "turbo lint --filter=@open-swe/web",
    "dev:api": "turbo dev --filter=@open-swe/api",
    "build:api": "turbo build --filter=@open-swe/api",
    "test:api": "turbo test --filter=@open-swe/api",
    "db:migrate": "cd apps/api && npx prisma migrate dev --name init",
    "test": "turbo test",
    "lint": "turbo lint",
    "build": "turbo build"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5"
  },
  "resolutions": {
    "@langchain/langgraph-sdk": "^0.0.95",
    "@langchain/core": "^0.3.65",
    "langsmith": "^0.3.48",
    "jsonwebtoken": "^9.0.2"
  }
}
